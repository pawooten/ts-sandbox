{
  "name": "ts-sandbox",
  "version": "1.0.0",
  "description": "A sandbox for TypeScript snippets and short programs",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paul Wooten",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.6.3"
  }
}
